#include <stdio.h>
void main(){
    int number ;
    printf("Enter the Number for Check \n");
    scanf("%d",&number);
    if(number>=0)
    printf("Number is Positive ");
    if(number<0)
    {
        printf("Number is Negative");
    }
}